package oblivion.v2.ui.theme

import androidx.compose.ui.graphics.Color

// ══════════════════════════════════════════════════════════════════
// Palette Oblivion — dark red / black, inspirée du logo
// ══════════════════════════════════════════════════════════════════

// Rouge principal (logo ≈ #E53935)
val OblivionRed = Color(0xFFE53935)
val OblivionRedLight = Color(0xFFEF5350)      // hover / accent clair
val OblivionRedDark = Color(0xFFC62828)       // pressed / containers

// Noirs & gris
val OblivionBlack = Color(0xFF0A0A0A)         // fond principal
val OblivionSurface = Color(0xFF141414)       // cartes / surfaces
val OblivionSurfaceVariant = Color(0xFF1C1C1C) // surfaces secondaires
val OblivionOnSurface = Color(0xFFE0E0E0)     // texte principal
val OblivionOnSurfaceVariant = Color(0xFF9E9E9E) // texte secondaire

// Containers
val OblivionPrimaryContainer = Color(0xFF3C1111)    // container rouge sombre
val OblivionOnPrimaryContainer = Color(0xFFFFCDD2)  // texte sur container rouge
val OblivionSecondaryContainer = Color(0xFF2A1A1A)  // container trigger ON
val OblivionOnSecondaryContainer = Color(0xFFEF9A9A) // texte sur container trigger
val OblivionErrorContainer = Color(0xFF4A0E0E)      // admin OFF / warnings
val OblivionOnErrorContainer = Color(0xFFFFB4AB)

// Outline
val OblivionOutline = Color(0xFF3A3A3A)
val OblivionOutlineVariant = Color(0xFF2A2A2A)
